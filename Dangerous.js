// The following code couldn't work in Browser
const fs = require("fs")
fs.readFileSync("/home/yvan/.ssh/id_rsa")

// The following code couldn't work in Node
// document.getElementById("main-container")
